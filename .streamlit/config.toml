[theme]
primaryColor = "#f44336"  # 기본 버튼 색상
backgroundColor = "#FFFFFF"  # 전체 배경색
secondaryBackgroundColor = "#F0F2F6"  # 사이드바 배경색
textColor = "#000000"  # 텍스트 색상(검정색)
font = "sans serif"  # 기본 글꼴 설정


[server]
runOnSave = true    # 파일을 저장할 때 앱을 자동으로 다시 실행